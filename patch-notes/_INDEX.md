# Patch notes - MMO Skill Stations Pack

Per-version public release notes (newest first). Same paradigm as the main mod repo: YAML frontmatter (`version`/`type: patch-note`/`status`), user-facing voice, no em-dashes. This pack has no `CURSEFORGE.md` yet; `README.md` is its public description.

- [[1.0.0]] - draft, pending release alongside plugin 1.6.0: first release, the Sawmill and the Anvil plus the new Smithing and Cooking skills.
